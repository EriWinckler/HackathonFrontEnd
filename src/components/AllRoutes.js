import React from "react";

function AllRoutes() {
  return (
    <section id="routes">
      <div>
        {
          // ticket routes:
          // /tickets
          // /active
          // /{id}
          // @DeleteMapping("/{id}")
          // @PostMapping
          // user on main folder
          //   /users
          //   @PostMapping - adduser
          //   @PutMapping("/resetpw/{id}")
          //   @DeleteMapping("/{id}")
        }
      </div>
    </section>
  );
}

export default AllRoutes;
